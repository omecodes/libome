package addresses

const (
	// CAServer Ome CA server address
	CAServer = "ca.ome.ci"

	// AccountsServer is the Ome Accounts server address
	AccountsServer = "accounts.ome.ci"

	// JWTStore is the Ome list of valid delivered JWTs store
	JWTStore = "jwt.ome.ci"
)
